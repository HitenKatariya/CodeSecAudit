# Root requirements for Vercel (@vercel/python installs this file).
# Website-only deps. Backend / rag-service deploy elsewhere (Render/HF)
# and are reached via CODESEC_API_URL / CODESEC_RAG_SERVICE_URL.
flask>=3.0.0
requests>=2.31.0
pymongo>=4.5.0
python-dotenv>=1.0.0
itsdangerous>=2.0.0
